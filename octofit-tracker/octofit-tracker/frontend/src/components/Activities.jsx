import ResourcePage from './ResourcePage.jsx'

export default function Activities() {
  return <ResourcePage endpoint="/api/activities/" resource="activities" title="Activities" description="A clear record of every effort." />
}